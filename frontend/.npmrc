# Use the domestic mirror for fast installs behind GFW.
# (The npm cache location is set per-run by run.bat / run.sh via
#  npm_config_cache=.../.tmp/npm-cache so nothing lands in ~/.npm.)
registry=https://registry.npmmirror.com
fund=false
audit=false
fetch-retries=5
fetch-timeout=60000
